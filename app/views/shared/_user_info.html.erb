
<h3>
  <%= current_user.name %>
</h3>
<div class="content">
  <!-- will need to be polished -->	
  <%= link_to "Create new post", new_advertisement_path %>
<h5>Categories</h5>
<% @tags.each do |tag| %>
  <ul><%= link_to tag, tag_path(tag.name) %></ul>
<% end %>
</div>